apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config-map
  namespace: cryptopulse
data:
  DB_HOST: postgres-service       # adresse PostgreSQL dans Kubernetes
  DB_PORT: "5432"                 
  POSTGRES_DB: cryptopulse        # pour l'image PostgreSQL officielle