apiVersion: v1
kind: Service
metadata:
  name: postgres-service  
spec:
  selector:
    app: postgres         # pointe vers les Pods qui ont le label app: postgres
  ports:
  - port: 5432         
    targetPort: 5432      